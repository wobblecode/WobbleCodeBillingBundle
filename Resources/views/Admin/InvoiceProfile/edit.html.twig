{% extends 'WobbleCodeAdminBundle::layout.html.twig' %}

{% block app_content_title %}
  {{ 'WobbleCodeBilling.view.Admin.InvoiceProfile.edit.title'|trans({'%id%': entity.id}) }}
{% endblock %}

{% block app_content_content %}

  <div class="row">
    <div class="col-sm-7">
      <p class="lead">{{ 'WobbleCodeBilling.view.Admin.InvoiceProfile.edit.subtitle'|trans({'%id%': entity.id}) }}</p>

      <form action="{{ path('admin_billing_invoice-profile_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
      <hr/>
        <button class="btn btn-primary" type="submit">{{ 'WobbleCodeBilling.view.Admin.InvoiceProfile.edit.button.edit'|trans }}</button>
        <a class="btn btn-link" href="{{ path('admin_billing_invoice-profile') }}">
          {{ 'WobbleCodeBilling.view.Admin.InvoiceProfile.edit.link.back'|trans }}
        </a>
        <button onclick="$('#form_delete_{{ delete_form.vars.name  }}').submit();" class="btn btn-danger pull-right" type="submit">{{ 'WobbleCodeBilling.view.Admin.InvoiceProfile.edit.button.delete'|trans }}</button>
      </form>

      <form id="form_delete_{{ delete_form.vars.name  }}" action="{{ path('admin_billing_invoice-profile_delete', { 'id': entity.id }) }}" method="post">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
      </form>

    </div>
  </div>

{% endblock %}
