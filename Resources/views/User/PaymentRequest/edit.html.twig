{% extends 'WobbleCodeBillingBundle::layout.html.twig' %}

{% block app_content_title %}
  {{ 'WobbleCodeBilling.view.User.PaymentRequest.edit.title'|trans({'%id%': entity.id}) }}
{% endblock %}

{% block app_content_content %}

  <div class="row">
    <div class="col-sm-7">
      <p class="lead">{{ 'WobbleCodeBilling.view.User.PaymentRequest.edit.subtitle'|trans({'%id%': entity.id}) }}</p>

      <form action="{{ path('billing_payment-request_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
      <hr/>
        <button class="btn btn-primary" type="submit">{{ 'WobbleCodeBilling.view.User.PaymentRequest.edit.button.edit'|trans }}</button>
        <a class="btn btn-link" href="{{ path('billing_payment-request') }}">
          {{ 'WobbleCodeBilling.view.User.PaymentRequest.edit.link.back'|trans }}
        </a>
        <button onclick="$('#form_delete_{{ delete_form.vars.name  }}').submit();" class="btn btn-danger pull-right" type="submit">{{ 'WobbleCodeBilling.view.User.PaymentRequest.edit.button.delete'|trans }}</button>
      </form>

      <form id="form_delete_{{ delete_form.vars.name  }}" action="{{ path('billing_payment-request_delete', { 'id': entity.id }) }}" method="post">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
      </form>

    </div>
  </div>

{% endblock %}
